#include "http-requestmethod.h"

const char HTTP_OPTIONS[] = "OPTIONS";
const size_t HTTP_OPTIONS_SIZE = sizeof(HTTP_OPTIONS) - 1;

const char HTTP_GET[] = "GET";
const size_t HTTP_GET_SIZE = sizeof(HTTP_GET) - 1;

const char HTTP_HEAD[] = "HEAD";
const size_t HTTP_HEAD_SIZE = sizeof(HTTP_HEAD) - 1;

const char HTTP_POST[] = "POST";
const size_t HTTP_POST_SIZE = sizeof(HTTP_POST) - 1;

const char HTTP_PUT[] = "PUT";
const size_t HTTP_PUT_SIZE = sizeof(HTTP_PUT) - 1;

const char HTTP_DELETE[] = "DELETE";
const size_t HTTP_DELETE_SIZE = sizeof(HTTP_DELETE) - 1;

const char HTTP_TRACE[] = "TRACE";
const size_t HTTP_TRACE_SIZE = sizeof(HTTP_TRACE) - 1;

const char HTTP_CONNECT[] = "CONNECT";
const size_t HTTP_CONNECT_SIZE = sizeof(HTTP_CONNECT) - 1;
